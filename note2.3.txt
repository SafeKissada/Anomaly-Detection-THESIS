================================================================================
B. [2.3] เกณฑ์เลือกโมเดล vs เกณฑ์รายงานผลคำนวณบนคนละ resolution
   ไฟล์ที่แก้: src/engine.py
   สถานะ: แก้ไขแล้ว
================================================================================

--------------------------------------------------------------------------------
B.1 ปัญหาเดิม
--------------------------------------------------------------------------------
มีสอง "จุด" ในโค้ดที่คำนวณ anomaly score จาก error map เดียวกันในเชิงแนวคิด แต่
ทำ post-processing ไม่เหมือนกัน:

  จุดที่ 1 (training-time) — train_autoencoder(): error_map_raw จาก
    elementwise_error_map() มีขนาด "ดิบ" ตามความละเอียดของ feature map จริง
    (เช่น 28x28 หรือ 7x7 ขึ้นกับ backbone) แล้วถูกส่งเข้า aggregate_score_torch()
    โดยตรง "ไม่มีการ upsample และไม่มี Gaussian smooth" ผลลัพธ์ (val_auroc) ถูก
    ใช้เป็น monitor ของ EarlyStopping (ค่า default cfg.AE_MONITOR='val_auroc')
    เพื่อตัดสินว่า checkpoint epoch ไหนคือ "ดีที่สุด"

  จุดที่ 2 (final evaluation) — score_dataset_split() -> process_single_heatmap():
    error map เดียวกันถูก cv2.resize ขยายเป็น cfg.IMAGE_SIZE (เช่น 224x224) ก่อน
    แล้วค่อย gaussian_filter(sigma=cfg.HEATMAP_SIGMA) แล้วค่อย aggregate_score()
    ด้วย SCORE_METHOD เดียวกัน (เช่น top-10%) ผลลัพธ์ใช้รายงานเป็น AUC-ROC/F1
    สุดท้ายใน final_results.json

แม้ SCORE_METHOD จะเป็นตัวเดียวกัน (เช่น topk 10%) แต่ "จำนวนพิกเซลที่ 10%
หมายถึง" ต่างกันคนละระดับ (10% ของ 28x28=784 พิกเซล เทียบกับ 10% ของ
224x224=50,176 พิกเซลหลัง interpolate) และ Gaussian smoothing (sigma=4) เปลี่ยน
การกระจายตัวของค่า error อย่างมีนัยสำคัญ (ลด peak ค่าสูงสุด กระจายพลังงานออก
รอบข้าง)

--------------------------------------------------------------------------------
B.2 เหตุผลเชิงทฤษฎีที่ใช้ในการแก้ไข
--------------------------------------------------------------------------------
หลักการพื้นฐานของ model selection ใน machine learning: criterion ที่ใช้ตัดสินใจ
เลือกโมเดล (validation criterion) ต้องเป็นตัวประมาณที่ไม่เอนเอียง (unbiased
estimator) ของ criterion ที่จะใช้วัดผลจริงตอน deployment/test หากทั้งสองคำนวณ
จากฟังก์ชันคนละตัวกัน ค่า validation ที่ดีที่สุดไม่รับประกันว่าจะให้ผลลัพธ์ test
ที่ดีที่สุด

ในเชิงคณิตศาสตร์ resize (bilinear interpolation) และ Gaussian smoothing เป็น
"การแปลงข้อมูลแบบไม่เชิงเส้นต่อการจัดอันดับเชิงพื้นที่ (non-linear w.r.t.
spatial ranking)" ของค่า error ดังนั้นการจัดอันดับภาพ (ranking) ตาม score แบบ
raw กับแบบ upsampled+smoothed "ไม่จำเป็นต้องตรงกัน" — โมเดล A ที่ให้ val_auroc
(raw) สูงสุด อาจไม่ใช่โมเดลที่ให้ AUC-ROC (upsampled+smoothed) สูงสุดจริง

วิธีแก้ที่ถูกต้องตามหลักทฤษฎี: ทำให้ทั้งสองจุด "เรียกใช้ฟังก์ชันเดียวกันตัวเป๊ะ ๆ"
(ไม่ใช่แค่เขียน logic คล้ายกันแยกกันสองที่ ซึ่งเสี่ยง diverge ในอนาคตถ้ามีคนแก้
จุดเดียวแล้วลืมอีกจุด) วิธีนี้พิสูจน์ได้ทางคณิตศาสตร์ว่า "อันดับที่ใช้เลือกโมเดล"
กับ "อันดับที่ใช้รายงานผล" เหมือนกันเสมอ ไม่มีทางเบี่ยงเบนกันอีก

--------------------------------------------------------------------------------
B.3 โค้ดก่อนแก้ไข (src/engine.py)
--------------------------------------------------------------------------------
--- ส่วนที่ 1: process_single_heatmap() (เดิมมี resize+smooth ฝังอยู่ในตัวเอง) ---

  def process_single_heatmap(
      feat_t       :  torch.Tensor,       # [C, H, W] tensor
      recon_t      :  torch.Tensor,       # [C, H, W] tensor
      sigma        :  float,
      out_size     :  Tuple[int,int],
      criterion    :  nn.Module = None
  ) -> Tuple[np.ndarray, np.ndarray]:
      if criterion is not None and isinstance(criterion, (SSIMLoss, CombinedLoss)):
        err_map = criterion.dissimilarity_map(
            recon_t.unsqueeze(0), feat_t.unsqueeze(0)
        ).squeeze(0).numpy()
      else:
        err_map = ((feat_t - recon_t) ** 2).mean(dim=0).numpy()

      score_map_up = cv2.resize(
          err_map,
          (out_size[1], out_size[0]),
          interpolation=cv2.INTER_LINEAR)

      raw_map = gaussian_filter(score_map_up, sigma=sigma)

      s_min, s_max = raw_map.min(), raw_map.max()
      normalized_map = (raw_map - s_min) / ((s_max - s_min) + 1e-8)

      return raw_map, normalized_map

--- ส่วนที่ 2: validation loop ใน train_autoencoder() (ใช้ aggregate_score_torch
    บน error map ขนาดดิบตรง ๆ ไม่ upsample ไม่ smooth) ---

          v_loss = criterion(recon, feats)
          val_loss += v_loss.item() * feats.size(0)
          error_map_raw   = elementwise_error_map(feats, recon, criterion)   # [B, H, W]
          per_sample_loss = error_map_raw.mean(dim=(1, 2))                    # [B]
          error_map_v     = aggregate_score_torch(error_map_raw, cfg)         # [B]

          batch_y = np.array([1 if l == 'anomaly' else 0 for l in batch_labels])
          normal_mask = (batch_y == 0)
          if normal_mask.any():
            normal_loss_sum += per_sample_loss.cpu().numpy()[normal_mask].sum()
            normal_loss_n   += int(normal_mask.sum())

          all_val_scores.extend(error_map_v.cpu().numpy().tolist())
          all_val_targets.extend(batch_y.tolist())

--------------------------------------------------------------------------------
B.4 โค้ดหลังแก้ไข (src/engine.py)
--------------------------------------------------------------------------------
--- ส่วนที่ 1: ฟังก์ชันกลางใหม่ upsample_and_smooth() + process_single_heatmap()
    ที่เรียกใช้ฟังก์ชันกลางนี้แทนโค้ด resize+smooth เดิมที่เคยฝังอยู่ในตัวเอง ---

  def upsample_and_smooth(
      err_map  : np.ndarray,
      sigma    : float,
      out_size : Tuple[int, int]
  ) -> np.ndarray:
      """Resize a native-resolution error map up to out_size then Gaussian-smooth it.

      This is the SINGLE shared post-processing step between:
        (a) training-time validation scoring (train_autoencoder -> val_auroc,
            used by EarlyStopping to select the checkpoint), and
        (b) final scoring (score_dataset_split -> process_single_heatmap,
            used for the reported test/val/train AUC-ROC, F1, etc.)

      Both call-sites MUST route through this exact function (not a
      reimplementation) so that the criterion used to pick the "best" model
      is provably the same criterion used to report its final performance.
      Resizing (bilinear) and Gaussian blur are both non-linear w.r.t. the
      spatial ranking of error values, so computing them at only one of the
      two call-sites would let the two rankings diverge silently.
      """
      score_map_up = cv2.resize(
          err_map,
          (out_size[1], out_size[0]),
          interpolation=cv2.INTER_LINEAR)
      return gaussian_filter(score_map_up, sigma=sigma)


  def process_single_heatmap(
      feat_t       :  torch.Tensor,       # [C, H, W] tensor
      recon_t      :  torch.Tensor,       # [C, H, W] tensor
      sigma        :  float,
      out_size     :  Tuple[int,int],
      criterion    :  nn.Module = None
  ) -> Tuple[np.ndarray, np.ndarray]:
      if criterion is not None and isinstance(criterion, (SSIMLoss, CombinedLoss)):
        err_map = criterion.dissimilarity_map(
            recon_t.unsqueeze(0), feat_t.unsqueeze(0)
        ).squeeze(0).numpy()
      else:
        err_map = ((feat_t - recon_t) ** 2).mean(dim=0).numpy()

      raw_map = upsample_and_smooth(err_map, sigma=sigma, out_size=out_size)

      s_min, s_max = raw_map.min(), raw_map.max()
      normalized_map = (raw_map - s_min) / ((s_max - s_min) + 1e-8)

      return raw_map, normalized_map

--- ส่วนที่ 2: validation loop ใน train_autoencoder() แก้ให้เรียก
    upsample_and_smooth() + aggregate_score() ตัวเดียวกันเป๊ะกับที่
    score_dataset_split() ใช้ แทน aggregate_score_torch() บนขนาดดิบ ---

          v_loss = criterion(recon, feats)
          val_loss += v_loss.item() * feats.size(0)
          error_map_raw   = elementwise_error_map(feats, recon, criterion)   # [B, H, W] native resolution
          per_sample_loss = error_map_raw.mean(dim=(1, 2))                    # [B] (diagnostic only, unaffected)

          # IMPORTANT (fix for the train/eval scoring-resolution mismatch):
          # The anomaly score used for val_auroc (which drives EarlyStopping /
          # checkpoint selection via cfg.AE_MONITOR) must be computed with the
          # EXACT SAME post-processing pipeline as the final reported metrics
          # in score_dataset_split(): upsample to cfg.IMAGE_SIZE, Gaussian-smooth
          # with cfg.HEATMAP_SIGMA, then aggregate with cfg.SCORE_METHOD.
          # We therefore route every sample through upsample_and_smooth() +
          # aggregate_score() here instead of aggregate_score_torch() on the
          # raw, native-resolution map. This is done on CPU (cv2/scipy have no
          # GPU batched equivalent) which is slightly slower per epoch, but
          # only affects the validation pass, not the training forward/backward.
          error_map_raw_np = error_map_raw.detach().cpu().numpy()
          batch_scores = np.empty(error_map_raw_np.shape[0], dtype=np.float32)
          for i in range(error_map_raw_np.shape[0]):
            smoothed_map = upsample_and_smooth(
                error_map_raw_np[i],
                sigma=cfg.HEATMAP_SIGMA,
                out_size=cfg.IMAGE_SIZE)
            batch_scores[i] = aggregate_score(smoothed_map, cfg)

          batch_y = np.array([1 if l == 'anomaly' else 0 for l in batch_labels])
          normal_mask = (batch_y == 0)
          if normal_mask.any():
            normal_loss_sum += per_sample_loss.cpu().numpy()[normal_mask].sum()
            normal_loss_n   += int(normal_mask.sum())

          all_val_scores.extend(batch_scores.tolist())
          all_val_targets.extend(batch_y.tolist())

--- ส่วนที่ 3: aggregate_score_torch() กลายเป็น dead code หลังแก้ไข (ไม่ถูกเรียก
    จากที่ไหนอีก) — ไม่ได้ลบทิ้ง แค่ใส่ comment กำกับสาเหตุไว้ ---

  def aggregate_score_torch(error_map: torch.Tensor, cfg) -> torch.Tensor:
    # NOTE: no longer called by train_autoencoder() as of the fix for the
    # train/eval scoring-resolution mismatch (see upsample_and_smooth()).
    # This aggregated the RAW, native-resolution error map with no upsampling
    # or Gaussian smoothing, which made val_auroc (model-selection criterion)
    # inconsistent with the final reported AUC-ROC (which IS upsampled +
    # smoothed in score_dataset_split). Kept here only for reference /
    # backward-compatibility with any external script that may still import it.
    b = error_map.size(0)
    flat = error_map.reshape(b, -1)
    if cfg.SCORE_METHOD == 'mean':
      return flat.mean(dim=1)
    elif cfg.SCORE_METHOD == 'max':
      return flat.max(dim=1).values
    elif cfg.SCORE_METHOD == 'topk':
      k = max(1, int(flat.size(1) * cfg.SCORE_TOPK_PERCENT / 100.0))
      topk_vals, _ = flat.topk(k, dim=1)
      return topk_vals.mean(dim=1)
    else:
      raise ValueError(f'Unknown SCORE_METHOD: {cfg.SCORE_METHOD!r}')

--------------------------------------------------------------------------------
B.5 ผลการทดสอบยืนยัน
--------------------------------------------------------------------------------
1) ทดสอบรัน train_autoencoder() แบบ end-to-end ด้วยข้อมูลจำลอง (feature 8ch@7x7,
   2 epoch, ผสม label normal/anomaly ใน val set) — รันสำเร็จไม่มี error:

     Epoch [   1/2]  TrainLoss=1.2822  ValLoss=1.2843  ValLossNormal=1.2721
                     ValAUROC=0.6667  Monitor[val_auroc]=0.6667  ...
     Epoch [   2/2]  TrainLoss=1.2519  ValLoss=1.2685  ValLossNormal=1.2904
                     ValAUROC=0.4167  Monitor[val_auroc]=0.4167  ...
     ✓ Best autoencoder loaded from .../best_autoencoder.pth  (monitor=val_auroc)
     checkpoint ถูกเซฟจริง: True

2) ทดสอบพิสูจน์ความสอดคล้อง (consistency proof) — คำนวณ score จาก feature/recon
   คู่เดียวกันผ่านสองเส้นทาง:
     ทาง A: เส้นทางที่ train_autoencoder ใช้ (validation loop ใหม่)
             -> upsample_and_smooth() + aggregate_score()
     ทาง B: เส้นทางที่ score_dataset_split ใช้จริง (final evaluation)
             -> process_single_heatmap() + aggregate_score()

     ผลลัพธ์:
       score จาก training-time path : 0.13998858630657196
       score จาก evaluation path     : 0.13998858630657196
       เท่ากันเป๊ะ (numpy.isclose)   : True

   สรุป: พิสูจน์ทางคณิตศาสตร์ได้แล้วว่าเกณฑ์เลือกโมเดล (val_auroc) กับเกณฑ์
   รายงานผลสุดท้าย (test AUC-ROC) คำนวณจากฟังก์ชันเดียวกันเป๊ะ ไม่มีทาง
   เบี่ยงเบนกันอีกต่อไป

--------------------------------------------------------------------------------
B.6 อิมแพคของการแก้ไข
--------------------------------------------------------------------------------
  ไฟล์ที่แก้                 : src/engine.py ไฟล์เดียว
  ฟังก์ชันที่เพิ่ม            : upsample_and_smooth() — จุดกลางที่ทั้งสอง path
                               ต้องเรียกร่วมกัน
  ฟังก์ชันที่แก้              : process_single_heatmap() (เรียก
                               upsample_and_smooth() แทนโค้ด resize+smooth เดิม
                               ที่เคยเขียนอยู่ในตัวมันเอง), validation loop ใน
                               train_autoencoder()
  ฟังก์ชันที่กลายเป็น dead code : aggregate_score_torch() (ไม่ถูกเรียกจากที่ไหน
                               อีก ใส่ comment กำกับไว้ ไม่ได้ลบทิ้ง)
  ไฟล์อื่นที่ต้องแก้เพิ่ม      : ไม่มี — score_dataset_split(), scripts/train.py,
                               scripts/visualize.py, src/losses.py ไม่ต้องแตะ
                               เลย เพราะ score_dataset_split() เรียก
                               process_single_heatmap() อยู่แล้วซึ่งตอนนี้ไปเรียก
                               upsample_and_smooth() แบบเดียวกันโดยอัตโนมัติ
  ผลต่อตัวเลข (numeric impact) : val_auroc ที่ log ระหว่างเทรนจะเปลี่ยนค่าไปจาก
                               เดิม (คำนวณบน map ที่ smooth แล้วแทนที่จะเป็น raw)
                               -> EarlyStopping อาจเลือก epoch/checkpoint ต่าง
                               จากเดิม -> checkpoint ที่ได้ best_autoencoder.pth
                               อาจไม่ใช่ตัวเดิมกับที่เคยรันมาก่อนแก้
  ผลต่อความเร็ว (performance)  : ช้าลงเล็กน้อยตอน validation เท่านั้น (เพิ่ม
                               cv2.resize + gaussian_filter ต่อภาพต่อ epoch,
                               ทำงานบน CPU เพราะ cv2/scipy ไม่มีเวอร์ชัน GPU แบบ
                               batched) — ไม่กระทบ training loop (forward/
                               backward ของ AE ยังเหมือนเดิมทุกอย่าง)
  ผลต่อ E0-E8 ที่เคยรันไปแล้ว   : ต้องรันใหม่ทั้งหมดถ้าจะใช้ตัวเลขจากโค้ด
                               เวอร์ชันนี้ในวิทยานิพนธ์ (แนะนำให้รันพร้อมกับการ
                               แก้ SSIM ในหัวข้อ A ข้างต้นในรอบเดียวกัน เพื่อรัน
                               ซ้ำแค่ครั้งเดียว)
